<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- Estilo Relatives comunes. -->
    <style name="RelativesLayoutComun">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    <!-- Estilo Linear búsqueda libros.  -->
    <style name="LinearBusqueda">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:background">@color/fondo</item>
    </style>
    <!-- Estilo EditText.  -->
    <style name="EditText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textColorHint">@color/blanco</item>
        <item name="android:textColor">@color/blanco</item>
    </style>

    <style name="BotonesBusqueda">
        <item name="android:layout_width">@dimen/botones_busqueda</item>
        <item name="android:layout_height">@dimen/botones_busqueda</item>
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:layout_toRightOf">@id/edit_text</item>
    </style>
    <!-- Estilo para las view comunes. -->
    <style name="Views">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">3dp</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>
    <!-- Estilo para el ListView.  -->
    <style name="ListView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:divider">@color/colorPrimaryDark</item>
        <item name="android:dividerHeight">2dp</item>
        <item name="android:orientation">vertical</item>
        <item name="android:layout_below">@id/view</item>
    </style>
    <!-- Estilo para la imagen de fondo.  -->
    <style name="ImagenFondo">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:scaleType">fitXY</item>
        <item name="android:src">@drawable/libros_variados</item>
        <item name="android:layout_below">@id/view</item>
    </style>
    <!-- Estilo para el texto de vista vacía.  -->
    <style name="ViewEmptyText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:padding">16dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Large</item>
        <item name="android:textColor">@color/fondo</item>
    </style>
    <!-- Estilo para el título del libro.  -->
    <style name="TituloLibro">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textAppearance">
            @style/TextAppearance.AppCompat.Light.SearchResult.Title
        </item>
        <item name="android:textColor">@color/blanco</item>
        <item name="android:layout_marginTop">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_marginBottom">@dimen/activity_horizontal_margin</item>
        <item name="android:background">@color/fondo</item>
    </style>
    <!-- Estilo para la portada del libro. -->
    <style name="PortadaLibro">
        <item name="android:layout_width">@dimen/ancho_portada</item>
        <item name="android:layout_height">@dimen/alto_portada</item>
        <item name="android:minHeight">@dimen/alto_portada</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:layout_marginBottom">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_below">@id/titulo</item>
    </style>
    <!-- Estilo para los autores del libro. -->
    <style name="AutoresLibro">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:textStyle">bold|italic</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:layout_below">@id/titulo</item>
        <item name="android:layout_toRightOf">@id/portada</item>
    </style>
    <!-- Estilo para el relative de la info.  -->
    <style name="RelativeInfoBook">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_toRightOf">@id/portada</item>
        <item name="android:layout_below">@id/autors</item>
    </style>
    <!-- Estilo para la info del libro.  -->
    <style name="InfoBook">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/blanco</item>
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Small</item>
        <item name="android:layout_marginLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_marginRight">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>
</resources>
